@model IEnumerable<AD___2FA.Models.TbCurriculo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Graduacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnosExperiencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CursosRealizados)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Graduacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnosExperiencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CursosRealizados)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCurriculo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCurriculo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCurriculo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
