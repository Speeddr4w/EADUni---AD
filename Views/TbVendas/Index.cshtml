@model IEnumerable<AD___2FA.Models.TbVendas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdClienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCursosNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdClienteNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCursosNavigation.Ementa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVendas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdVendas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVendas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
